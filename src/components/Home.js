import DrawCard from "./cards/DrawCard"

const Home = (props) => {
    // console.log('home props', props)
    return(
        <div>
            <DrawCard />
        </div>
    )
}

export default Home