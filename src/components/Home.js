import DrawCard from "./cards/DrawCard"

const Home = (props) => {
    // console.log('home props', props)
    return(
        <div class="container">
            <DrawCard />
        </div>
    )
}

export default Home